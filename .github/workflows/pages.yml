name: Publish Eat What?

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check for a Google API key
        run: |
          if grep -R -E -q 'AIza[0-9A-Za-z_-]+' src assets scripts index.html privacy.html terms.html sw.js; then
            echo 'A Google API key pattern was found. Publication stopped.'
            exit 1
          fi
      - run: npm test
      - run: BASE_PATH=/eat-what/ npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: dist
      - name: Publish to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
